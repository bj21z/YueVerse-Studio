# V3.0.0 发布说明

本次为架构迁移版本，必须新建或全量替换仓库。旧的两个独立仓库可保留作为回滚备份。

## 新增
根首页、共享组件、统一资讯 API、统一更新脚本、统一 GitHub Actions、演员注册表、工程检查脚本及完整文档。

## 修改
两款演员应用的资讯请求改为根级 `/api/news?actor=...`，并加载统一品牌组件。

## 删除
演员子项目中的重复 `functions/`、`scripts/` 与 `.github/`；对应能力已提升到根目录共享。
